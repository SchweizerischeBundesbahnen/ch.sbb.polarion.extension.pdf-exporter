<?xml version="1.0" encoding="UTF-8"?>
<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.sbb.polarion.extensions</groupId>
        <artifactId>ch.sbb.polarion.extension.generic</artifactId>
        <version>16.1.0</version>
    </parent>

    <artifactId>ch.sbb.polarion.extension.pdf-exporter</artifactId>
    <version>13.8.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Polarion ALM extension to convert Documents to PDF files</name>
    <description>This Polarion extension provides possibility to convert Polarion Documents to PDF files. This is an alternative to native Polarion's solution.</description>
    <url>https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.pdf-exporter</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>SBB Polarion Team</name>
            <email>polarion-opensource@sbb.ch</email>
            <organization>SBB AG</organization>
            <organizationUrl>https://www.sbb.ch</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.pdf-exporter.git</connection>
        <developerConnection>scm:git:ssh://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.pdf-exporter.git</developerConnection>
        <url>https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.pdf-exporter/tree/main</url>
    </scm>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.pdf-exporter/issues</url>
    </issueManagement>

    <properties>
        <pdfbox.version>3.0.8</pdfbox.version>
        <okapi.xliff.version>1.48.0</okapi.xliff.version>
        <flying-saucer.version>10.5.0</flying-saucer.version>

        <slf4j.version>2.0.20</slf4j.version>
        <testcontainers.version>2.0.5</testcontainers.version>

        <awaitility.version>4.3.0</awaitility.version>
        <!-- Hold at 3.x: tika-core 4.0.0 ships the compile-time processor
             org/apache/tika/annotation/TikaComponentProcessor.class and an OSGi manifest exporting
             org.apache.tika.annotation with uses:="javax.annotation.processing,...". Polarion 2606's
             JakartaCompatibilityChecker rejects both a class referencing and a manifest value
             containing a forbidden package prefix, and "javax.annotation.processing" contains
             "javax.annotation" - so the extension jar counts as one incompatible file and Polarion
             refuses to start. See renovate.json. -->
        <tika.version>3.3.2</tika.version>
        <ph-css.version>8.2.1</ph-css.version>
        <commons-text.version>1.15.0</commons-text.version>
        <verapdf.version>1.30.2</verapdf.version>
        <rhino.version>1.9.1</rhino.version>


        <maven-jar-plugin.Extension-Context>pdf-exporter</maven-jar-plugin.Extension-Context>
        <maven-jar-plugin.Automatic-Module-Name>ch.sbb.polarion.extension.pdf_exporter</maven-jar-plugin.Automatic-Module-Name>
        <maven-jar-plugin.Discover-Base-Package>ch.sbb.polarion.extension.pdf_exporter</maven-jar-plugin.Discover-Base-Package>
        <maven-jar-plugin.Configuration-Properties-Prefix>ch.sbb.polarion.extension.pdf-exporter.</maven-jar-plugin.Configuration-Properties-Prefix>
        <web.app.name>${maven-jar-plugin.Extension-Context}</web.app.name>


        <!-- Extension-specific Require-Bundle (in addition to common ones from parent POM):
            com.polarion.alm.wiki
            org.jsoup
            net.bytebuddy.byte-buddy
            org.apache.tika.patched;resolution:=optional
            org.apache.tika;resolution:=optional
            org.apache.httpcomponents.httpclient, org.apache.httpcomponents.httpcore (used by
              CustomResourceUrlResolver to pin a request to the addresses ResourceUrlPolicy vetted)
        -->
        <maven-jar-plugin.Require-Bundle.extension>com.polarion.alm.wiki,org.jsoup,net.bytebuddy.byte-buddy,org.apache.tika.patched;resolution:=optional,org.apache.tika;resolution:=optional,org.apache.httpcomponents.httpclient,org.apache.httpcomponents.httpcore</maven-jar-plugin.Require-Bundle.extension>
        <maven-jar-plugin.Require-Bundle>${maven-jar-plugin.Require-Bundle.common},${maven-jar-plugin.Require-Bundle.extension}</maven-jar-plugin.Require-Bundle>
    </properties>


    <profiles>
        <profile>
            <id>tests-with-weasyprint-docker</id>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven-surefire-plugin.version}</version>
                        <configuration>
                            <!-- hide unwanted exceptions logging -->
                            <redirectTestOutputToFile>true</redirectTestOutputToFile>
                            <!-- Reuse JVM for all tests to enable container sharing -->
                            <forkCount>1</forkCount>
                            <reuseForks>true</reuseForks>
                            <!-- Run tests sequentially to ensure container is shared -->
                            <parallel>none</parallel>
                            <systemPropertyVariables>
                                <wpExporterImpl>docker</wpExporterImpl>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Override vulnerable transitive rhino from verapdf -->
            <dependency>
                <groupId>org.mozilla</groupId>
                <artifactId>rhino</artifactId>
                <version>${rhino.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Polarion's own httpcomponents bundles, resolved at runtime via Require-Bundle above
             and never packaged. Used by CustomResourceUrlResolver. -->
        <dependency>
            <groupId>com.polarion.thirdparty</groupId>
            <artifactId>org.apache.httpcomponents.httpclient_4.5.14</artifactId>
            <version>${polarion.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.polarion.thirdparty</groupId>
            <artifactId>org.apache.httpcomponents.httpcore_4.4.16</artifactId>
            <version>${polarion.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>ch.sbb.polarion.extensions</groupId>
            <artifactId>ch.sbb.polarion.extension.generic.app</artifactId>
            <version>${project.parent.version}</version>
        </dependency>

        <dependency>
            <groupId>com.polarion.thirdparty</groupId>
            <artifactId>org.apache.velocity.engine-core_2.3.0</artifactId>
            <version>${polarion.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.polarion.alm.wiki</groupId>
            <artifactId>sidecar</artifactId>
            <version>${polarion.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.polarion.thirdparty</groupId>
            <artifactId>org.jsoup_1.21.2</artifactId>
            <version>${polarion.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.polarion.thirdparty</groupId>
            <artifactId>net.bytebuddy.byte-buddy_1.18.3</artifactId>
            <version>${polarion.version}</version>
            <scope>provided</scope>
        </dependency>

        <!--  3rd-party libraries to be packaged into resulting jar  -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>
        <dependency>
            <groupId>net.sf.okapi.lib</groupId>
            <artifactId>okapi-lib-xliff2</artifactId>
            <version>${okapi.xliff.version}</version>
        </dependency>
        <dependency>
            <groupId>com.helger</groupId>
            <artifactId>ph-css</artifactId>
            <version>${ph-css.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-text</artifactId>
            <version>${commons-text.version}</version>
        </dependency>

        <dependency>
            <groupId>org.xhtmlrenderer</groupId>
            <artifactId>flying-saucer-core</artifactId>
            <version>${flying-saucer.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
            <version>${tika.version}</version>
        </dependency>

        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>validation-model-jakarta</artifactId>
            <version>${verapdf.version}</version>
            <!-- Jackson is provided by Polarion's OSGi container (2.19.0) at runtime and by the
                 parent POM's provided bundles at compile time. Exclude verapdf's transitive copy
                 so it isn't bundled and can't collide with Polarion's version. -->
            <exclusions>
                <exclusion>
                    <groupId>com.fasterxml.jackson.core</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!--  Test dependencies  -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Route SLF4J (used by flying-saucer) to Log4j2 so its logs can be captured by InMemoryAppender in tests -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <version>${log4j.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>${awaitility.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.sbb.polarion.extensions</groupId>
            <artifactId>ch.sbb.polarion.extension.generic.app</artifactId>
            <version>${project.parent.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <configuration>
                    <filesets>
                        <fileset>
                            <!-- Everything markdown2html renders below (the parent only knows about.html);
                                 the same files .gitignore keeps out of git. -->
                            <directory>${markdown2html-maven-plugin.extensionContextAdminHtml}</directory>
                            <includes>
                                <include>*.html</include>
                            </includes>
                            <followSymlinks>false</followSymlinks>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>

            <plugin>
                <groupId>ch.sbb.maven.plugins</groupId>
                <artifactId>markdown2html-maven-plugin</artifactId>
                <!--
                    Renders the About page (README.md), the Usage Disclaimer and the documentation-site articles
                    of ui/src/docs/docs.config.json into the app webapp's html/, which the app fetches them from.

                    The plugin-level configuration is shared by every execution: the parent's (failOnError,
                    generateHeadingIds, openExternalLinksInNewTab, its excludeChapters and removeLinesWithStrings)
                    plus the two below. An execution only names its input and output, and replaces the parent's
                    excludeChapters where it lists its own.

                    Every execution runs in generate-sources, one phase before the frontend build in
                    generate-resources: build-docs-index.mjs (npm `prebuild`) builds the documentation search
                    index from this very HTML, using the heading ids generateHeadingIds stamps on it, and fails
                    the build when an article is missing. An article added to the manifest needs an execution here.
                -->
                <configuration>
                    <!-- Keep relative links relative: the app's DocLinkInterceptor turns a link to another
                         article into in-app navigation, and any other one (docs/openapi.json) into a GitHub link.
                         combine.self=override is required: an empty element alone does not override the
                         parent's ${project.scm.url}, Maven keeps the inherited value on an empty merge. -->
                    <relativeLinkPrefix combine.self="override"></relativeLinkPrefix>
                    <!-- The articles are fetched as single files, so their images travel inside them. -->
                    <imageProcessingType>EMBED</imageProcessingType>
                </configuration>
                <executions>
                    <execution>
                        <id>readme.md-to-about.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <!-- The README's documentation table keeps a REST API row for GitHub, where the
                                 relative docs/openapi.json link resolves; drop that row from the generated About
                                 page, where the OpenAPI spec is already reachable from the REST API section below.
                                 combine.children=append keeps the parent's list (which strips the SonarCloud
                                 badges): without it this list would replace the inherited one and the badges would
                                 leak onto the About page. -->
                            <removeLinesWithStrings combine.children="append">
                                <removeLinesWithString>[REST API](docs/openapi.json)</removeLinesWithString>
                            </removeLinesWithStrings>
                        </configuration>
                    </execution>
                    <execution>
                        <id>disclaimer.md-to-disclaimer.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <inputFile>${project.basedir}/DISCLAIMER.md</inputFile>
                            <outputFile>${markdown2html-maven-plugin.extensionContextAdminHtml}/disclaimer.html</outputFile>
                        </configuration>
                    </execution>
                    <!-- The documentation-site articles, in the reading order of ui/src/docs/docs.config.json. -->
                    <execution>
                        <id>quick_start.md-to-quick-start.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <inputFile>${project.basedir}/QUICK_START.md</inputFile>
                            <outputFile>${markdown2html-maven-plugin.extensionContextAdminHtml}/quick-start.html</outputFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>user_guide.md-to-user-guide.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <inputFile>${project.basedir}/USER_GUIDE.md</inputFile>
                            <outputFile>${markdown2html-maven-plugin.extensionContextAdminHtml}/user-guide.html</outputFile>
                            <!-- The markdown keeps a Table of contents for GitHub; the sidebar and "On this page"
                                 of the documentation site provide that navigation, so drop it here. -->
                            <excludeChapters>
                                <excludeChapter>## Table of contents</excludeChapter>
                            </excludeChapters>
                        </configuration>
                    </execution>
                    <execution>
                        <id>configuration.md-to-configuration.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <inputFile>${project.basedir}/CONFIGURATION.md</inputFile>
                            <outputFile>${markdown2html-maven-plugin.extensionContextAdminHtml}/configuration.html</outputFile>
                            <!-- The markdown keeps a Table of contents for GitHub; the sidebar and "On this page"
                                 of the documentation site provide that navigation, so drop it here. -->
                            <excludeChapters>
                                <excludeChapter>## Table of contents</excludeChapter>
                            </excludeChapters>
                        </configuration>
                    </execution>
                    <execution>
                        <id>limitations.md-to-limitations.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <inputFile>${project.basedir}/LIMITATIONS.md</inputFile>
                            <outputFile>${markdown2html-maven-plugin.extensionContextAdminHtml}/limitations.html</outputFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>upgrade.md-to-upgrade.html</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>convert</goal>
                        </goals>
                        <configuration>
                            <inputFile>${project.basedir}/UPGRADE.md</inputFile>
                            <outputFile>${markdown2html-maven-plugin.extensionContextAdminHtml}/upgrade.html</outputFile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>ch.sbb.polarion.extensions</groupId>
                                    <artifactId>ch.sbb.polarion.extension.generic.app</artifactId>
                                    <version>${project.parent.version}</version>
                                    <outputDirectory>${project.build.directory}/generic-jar-content</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-maven-plugin-jakarta</artifactId>
                <configuration>
                    <outputFormat>JSON</outputFormat>
                    <!-- Stable ordering, so a rebuild does not reshuffle docs/openapi.json. -->
                    <resourcePackages>
                        <package>ch.sbb.polarion.extension.generic.rest.controller.info</package>
                        <package>ch.sbb.polarion.extension.generic.rest.controller.roles</package>
                        <package>ch.sbb.polarion.extension.generic.rest.controller.settings</package>
                        <package>ch.sbb.polarion.extension.generic.rest.model</package>
                        <package>ch.sbb.polarion.extension.pdf_exporter.rest.controller</package>
                        <package>ch.sbb.polarion.extension.pdf_exporter.rest.model</package>
                    </resourcePackages>
                </configuration>
            </plugin>
            <plugin>
                <groupId>io.github.grigoriev</groupId>
                <artifactId>pre-commit-run-maven-plugin</artifactId>
            </plugin>
            <!-- Opts in to the compatibility check the generic parent pom declares. -->
            <plugin>
                <groupId>com.intechcore</groupId>
                <artifactId>polarion-compatibility-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>
