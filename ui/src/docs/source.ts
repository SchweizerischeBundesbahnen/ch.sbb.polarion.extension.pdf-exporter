/**
 * The extension's sources on GitHub, as a blob view of `main`: a file link needs the `blob/<ref>` segment,
 * without it GitHub answers 404. Used for the "not generated" fallback links (a documentation article, the
 * Usage Disclaimer) and for the articles' links to repository files such as `docs/openapi.json`.
 */
export const SOURCE_BASE_URL =
  'https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.pdf-exporter/blob/main';
